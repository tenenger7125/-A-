export enum EnrollmentStep {
  COURSE = 'course',
  INFO = 'info',
  CONFIRM = 'confirm',
  COMPLETE = 'complete',
}

export enum EnrollmentApplicationType {
  INDIVIDUAL = 'individual',
  GROUP = 'group',
}
