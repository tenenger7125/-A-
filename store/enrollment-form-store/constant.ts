export enum EnrollmentStep {
  COURSE = 'course',
  INFO = 'info',
  CONFIRM = 'confirm',
  COMPLETE = 'complete',
}

export enum EnrollmentApplicationType {
  PERSONAL = 'personal',
  GROUP = 'group',
}
